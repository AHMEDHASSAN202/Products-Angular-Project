import { HeaderRedColorDirective } from './header-red-color.directive';

describe('HeaderRedColorDirective', () => {
  it('should create an instance', () => {
    const directive = new HeaderRedColorDirective();
    expect(directive).toBeTruthy();
  });
});
